<figure
  class="ascii-scene"
  aria-label="A tiny terminal collecting scattered ideas"
>
  <div class="ascii-sky" aria-hidden="true">
    <span class="spark spark-one">+</span>
    <span class="spark spark-two">*</span>
    <span class="spark spark-three">·</span>
    <pre>       .--------.
      /  .--.    \
  *  |  /    \    |  +
     |  \    /    |
      \  '--'    /
       '---.----'
           |
     .-----+------.
     | notes/*.md |
     '------------'
       imo@web:~$ <span class="cursor">_</span></pre>
  </div>
  <figcaption>signal found among the noise</figcaption>
</figure>

<style>
  .ascii-scene {
    position: relative;
    margin: 0;
    overflow: hidden;
    border: 1px solid var(--color-border-strong);
    border-radius: var(--radius-lg);
    color: var(--color-on-accent);
    background:
      linear-gradient(rgb(255 255 255 / 5%) 1px, transparent 1px),
      linear-gradient(90deg, rgb(255 255 255 / 5%) 1px, transparent 1px),
      var(--color-ink);
    background-size: 1.5rem 1.5rem;
    box-shadow: var(--shadow-lg);
  }

  .ascii-scene::after {
    position: absolute;
    inset: 0;
    background: linear-gradient(transparent 50%, rgb(0 0 0 / 9%) 50%);
    background-size: 100% 4px;
    content: "";
    pointer-events: none;
  }

  .ascii-sky {
    min-height: 24rem;
    display: grid;
    place-items: center;
    padding: var(--space-xl);
  }

  pre {
    margin: 0;
    color: #d9f5eb;
    font-family: var(--font-mono);
    font-size: clamp(0.72rem, 2.25vw, 1rem);
    line-height: 1.55;
    text-shadow: 0 0 1rem rgb(94 224 185 / 35%);
    animation: float 6s ease-in-out infinite;
  }

  .cursor {
    color: var(--color-highlight);
    animation: blink 1s steps(1, end) infinite;
  }

  .spark {
    position: absolute;
    z-index: 1;
    color: var(--color-highlight);
    font-family: var(--font-mono);
    font-size: 1.3rem;
    animation: twinkle 2.4s steps(2, end) infinite;
  }

  .spark-one {
    top: 13%;
    left: 17%;
  }

  .spark-two {
    top: 25%;
    right: 14%;
    animation-delay: -0.8s;
  }

  .spark-three {
    right: 25%;
    bottom: 23%;
    animation-delay: -1.6s;
  }

  figcaption {
    padding: var(--space-sm) var(--space-md);
    border-top: 1px solid rgb(255 255 255 / 14%);
    color: #a9b9b4;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    letter-spacing: 0.05em;
    text-align: center;
    text-transform: uppercase;
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0);
    }

    50% {
      transform: translateY(-0.55rem);
    }
  }

  @keyframes blink {
    50% {
      opacity: 0;
    }
  }

  @keyframes twinkle {
    50% {
      opacity: 0.25;
      transform: scale(0.75);
    }
  }

  @media (prefers-reduced-motion: reduce) {
    pre,
    .cursor,
    .spark {
      animation: none;
    }
  }

  @media (max-width: 48rem) {
    .ascii-sky {
      min-height: 19rem;
    }
  }
</style>
