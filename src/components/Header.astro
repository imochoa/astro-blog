---
import PagefindConfig from "astro-pagefind/components/PagefindConfig.astro";

const links = [
  { href: "/", label: "Home" },
  { href: "/blog/", label: "Blog" },
  { href: "/tags/", label: "Tags" },
];

const pathname = Astro.url.pathname;
const isActive = (href: string) =>
  href === "/" ? pathname === href : pathname.startsWith(href);
---

<header class="site-header">
  <nav class="container" aria-label="Primary navigation">
    <a class="site-name" href="/">astro-blog</a>
    <ul role="list">
      {
        links.map(({ href, label }) => (
          <li>
            <a href={href} aria-current={isActive(href) ? "page" : undefined}>
              {label}
            </a>
          </li>
        ))
      }
    </ul>
    <div class="site-search">
      <PagefindConfig instance="site-search" lang="en" />
      <pagefind-searchbox
        instance="site-search"
        placeholder="Search posts"
        max-results="6"
        show-sub-results></pagefind-searchbox>
    </div>
  </nav>
</header>

<style>
  .site-header {
    border-bottom: 1px solid var(--color-border);
    background: var(--color-surface);
  }

  nav {
    min-height: 4rem;
    display: grid;
    grid-template-columns: auto 1fr minmax(14rem, 20rem);
    align-items: center;
    gap: var(--space-md);
  }

  .site-name {
    color: var(--color-text);
    font-weight: 700;
    text-decoration: none;
  }

  ul {
    display: flex;
    justify-self: end;
    gap: var(--space-md);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  ul a {
    color: var(--color-muted);
    text-decoration: none;
  }

  ul a:hover,
  ul a[aria-current="page"] {
    color: var(--color-accent);
  }

  .site-search {
    --pf-background: var(--color-surface);
    --pf-border: var(--color-border);
    --pf-border-focus: var(--color-accent);
    --pf-border-radius: var(--radius);
    --pf-font: var(--font-sans);
    --pf-input-height: 2.5rem;
    --pf-mark: var(--color-accent-soft);
    --pf-searchbox-max-width: 20rem;
    --pf-text: var(--color-text);
    --pf-text-muted: var(--color-muted);
    width: 100%;
  }

  pagefind-searchbox {
    display: block;
  }

  @media (max-width: 48rem) {
    nav {
      grid-template-columns: auto 1fr;
      padding-block: var(--space-sm);
    }

    .site-search {
      grid-column: 1 / -1;
      --pf-searchbox-max-width: none;
    }
  }
</style>
