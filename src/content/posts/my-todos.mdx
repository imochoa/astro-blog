---
title: ToDo's
description: Some ideas
publishedAt: 2026-08-18
draft: true
tags:
  - astro
---

import { Image } from "astro:assets";
// import featureDiagram from "./assets/xournal.svg";

{/* <figure>
  <Image
    src={featureDiagram}
    alt="Markdown content flowing through Sätteri into an Astro page"
  />
  <figcaption>A local SVG rendered with Astro's Image component.</figcaption>
</figure> */}

http://localhost:4321/elements https://expressive-code.com/plugins/line-numbers/

- AI podcast summaries
- embedding diagrams using cog & plantuml

## Using pikchr

`nix-shell -p python311Packages.cogapp --command "cog -rec <file.svg>"` `-z` to
ignore `[[[end]]]` SVG file:

`nix-shell -p python311Packages.cogapp pikchr --command "cog -re test.svg" && cat test.svg`

```svg
<!--
[[[cog
import cog
from subprocess import Popen, PIPE, STDOUT

diagram="""
box move box
box move box
"""
diagram = "\n".join(diagram.split())

p = Popen(
    ["pikchr", "-"+"-svg-only", "-"],
    stdout=PIPE,
    stdin=PIPE,
    stderr=PIPE,
    text=True,
)
stdout_data = p.communicate(input=diagram)[0]
cog.out(stdout_data)
p.close()
# or p.exit()
]]]-->
<svg xmlns='http://www.w3.org/2000/svg' class="pikchr" viewBox="0 0 580.32 76.32">
...
</svg>
<!-- [[[end]]] -->
```

`nix-shell -p pikchr --command "pikchr /tmp/hoho.txt"`

- easy github actions with docker
- practical decorators talk
- vimcasts summaries
- docker CE vs docker IO
- using `{}` to separate traefik labels
- docker squashing & logging
- vim folding in files

```bash

# {{{1 GUI properties

# |  {{{1 Border props

#    |  {{{1 Top border

# vim:fdm=marker:fen:fdl=0:

```

will require highlighting!

nomenclature

https://knezevic.ch/posts/zsh-completion-for-tools-installed-via-home-manager/

https://github.com/imochoa/yadm/blob/master/.config/zsh/.zshrc

# talon

# catima

# syncthing

# justfile picker using fzf

## Using pikchr

`nix-shell -p python311Packages.cogapp --command "cog -rec <file.svg>"` `-z` to
ignore `[[[end]]]` SVG file:

`nix-shell -p python311Packages.cogapp pikchr --command "cog -re test.svg" && cat test.svg`

```svg
<!--
[[[cog
import cog
from subprocess import Popen, PIPE, STDOUT

diagram="""
box move box
box move box
"""
diagram = "\n".join(diagram.split())

p = Popen(
    ["pikchr", "-"+"-svg-only", "-"],
    stdout=PIPE,
    stdin=PIPE,
    stderr=PIPE,
    text=True,
)
stdout_data = p.communicate(input=diagram)[0]
cog.out(stdout_data)
p.close()
# or p.exit()
]]]-->
<svg xmlns='http://www.w3.org/2000/svg' class="pikchr" viewBox="0 0 580.32 76.32">
...
</svg>
<!-- [[[end]]] -->
```

`nix-shell -p pikchr --command "pikchr /tmp/hoho.txt"`

- easy github actions with docker
- practical decorators talk
- vimcasts summaries
- docker CE vs docker IO
- using `{}` to separate traefik labels
- docker squashing & logging
- vim folding in files

```bash

# {{{1 GUI properties

# |  {{{1 Border props

#    |  {{{1 Top border

# vim:fdm=marker:fen:fdl=0:

```

will require highlighting!

nomenclature
